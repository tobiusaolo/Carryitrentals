# CarryIT frontend — copy to .env.local for overrides
# Must use https:// on Render (http:// causes 307 redirects and CORS failures)
REACT_APP_API_URL=https://carryit-backend-su8h.onrender.com/api/v1
